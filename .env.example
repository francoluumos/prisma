# Frontend env for the Beta assistant. Copy to .env.local (gitignored) and fill in.
# Both are public/browser-safe; the Gemini key lives only in the Supabase function.
VITE_SUPABASE_URL=https://<your-project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<your-anon-key>
