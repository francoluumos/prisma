# Asset-only module: no Python. The file still has to exist, because Odoo
# imports every addon as a Python package and skips directories it cannot
# import — the module simply never appears in the apps list without it.
